import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#ffffff',
    alignItems: 'center',
  },
  containerConfig:{
    flexDirection: 'row',
    alignItems: 'flex-end',
    height: 80,
  },
});
